## Agent skills

### Issue tracker

Local markdown file tracking under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses canonical default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repository format. See `docs/agents/domain.md`.
